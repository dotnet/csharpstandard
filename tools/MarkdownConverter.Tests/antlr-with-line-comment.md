# 1 Heading

This is a grammar:

```ANTLR
reference_type
    // Comment before colon
    : class_type
    // Comment between rules
    | interface_type
    | array_type // Comment at end of line
    ;
```